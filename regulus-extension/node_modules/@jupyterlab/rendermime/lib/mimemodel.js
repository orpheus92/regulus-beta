"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * The default mime model implementation.
 */
var MimeModel = (function () {
    /**
     * Construct a new mime model.
     */
    function MimeModel(options) {
        if (options === void 0) { options = {}; }
        this.trusted = !!options.trusted;
        this._data = options.data || {};
        this._metadata = options.metadata || {};
    }
    Object.defineProperty(MimeModel.prototype, "data", {
        /**
         * The data associated with the model.
         */
        get: function () {
            return this._data;
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(MimeModel.prototype, "metadata", {
        /**
         * The metadata associated with the model.
         */
        get: function () {
            return this._metadata;
        },
        enumerable: true,
        configurable: true
    });
    /**
     * Set the data associated with the model.
     *
     * #### Notes
     * Depending on the implementation of the mime model,
     * this call may or may not have deferred effects,
     */
    MimeModel.prototype.setData = function (options) {
        this._data = options.data || this._data;
        this._metadata = options.metadata || this._metadata;
    };
    return MimeModel;
}());
exports.MimeModel = MimeModel;
